[project]
name = "agent-tooling"
version = "0.1.0"
description = "Reusable agent skills, personas, commands, hooks, and projection tooling"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
